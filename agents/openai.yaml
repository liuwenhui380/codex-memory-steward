interface:
  display_name: "Codex Memory Steward"
  short_description: "Steward tiered Codex memory, usage stats, and splitting"
  version: "1.1.3"
  default_prompt: "Use $codex-memory-steward to update project-local agent.md/.agent memory, recall only matching routes, record entries actually used, keep stats in one Markdown layer, and split oversized memory by concrete topic."
